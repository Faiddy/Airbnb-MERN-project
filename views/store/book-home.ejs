<%- include('../partials/head') %>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/intl-tel-input/17.0.19/css/intlTelInput.min.css" />
</head>
<body>
  <%- include('../partials/nav') %>

  <main class="container mx-auto max-w-lg mt-16 bg-white rounded-2xl shadow-xl p-8">
    <h1 class="text-3xl font-bold mb-6 text-center text-red-600">Book This Home</h1>

    <% if (home) { %>
      <div class="mb-6 text-center">
        <% if (home.photo) { %>
          <img src="<%= home.photo %>" alt="<%= home.houseName %>" style="width: 20%; height: auto;" class="object-cover rounded-xl shadow mx-auto" />
        <% } %>
        <h2 class="text-2xl font-semibold mt-4 mb-2"><%= home.houseName %></h2>
        <p class="text-gray-700 mb-2"><strong>Location:</strong> <%= home.location %></p>
        <p class="text-gray-700 mb-4"><strong>Price per night:</strong> Rs <%= home.price %></p>
      </div>

      <form action="/store/book" method="POST" class="space-y-4" id="bookingForm">
        <input type="hidden" name="homeId" value="<%= home._id %>" />

        <label class="block font-semibold">From Date</label>
        <input type="date" name="dateFrom" required class="w-full border rounded px-3 py-2" />

        <label class="block font-semibold">To Date</label>
        <input type="date" name="dateTo" required class="w-full border rounded px-3 py-2" />

        <label class="block font-semibold">Full Name</label>
        <input type="text" name="fullName" required class="w-full border rounded px-3 py-2" />

        <label class="block font-semibold">Email</label>
        <input type="email" name="email" required class="w-full border rounded px-3 py-2" />

        <label class="block font-semibold">Phone</label>
        <div class="phone-input-container" style="position: relative; width: 100%;">
          <select id="phone" name="phone" required class="w-full border rounded px-3 py-2">
            <option value="">Select Country Code</option>
            <option value="+92">🇵🇰 Pakistan (+92)</option>
            <option value="+1">🇺🇸 United States (+1)</option>
            <option value="+44">🇬🇧 United Kingdom (+44)</option>
            <option value="+1">🇨🇦 Canada (+1)</option>
            <option value="+61">🇦🇺 Australia (+61)</option>
            <option value="+91">🇮🇳 India (+91)</option>
          </select>
          <input type="tel" id="phoneNumber" name="phoneNumber" required class="w-full border rounded px-3 py-2 mt-2" placeholder="Enter phone number" />
          <small id="phoneFormat" class="text-gray-500 mt-1 block"></small>
        </div>

        <label class="block font-semibold">Nationality</label>
        <select id="nationality" name="nationality" required class="w-full border rounded px-3 py-2">
          <option value="">Select Nationality</option>
          <option value="Pakistan">🇵🇰 Pakistan</option>
          <option value="United States">🇺🇸 United States</option>
          <option value="United Kingdom">🇬🇧 United Kingdom</option>
          <option value="Canada">🇨🇦 Canada</option>
          <option value="Australia">🇦🇺 Australia</option>
          <option value="India">🇮🇳 India</option>
          <!-- Add more countries here -->
        </select>

        <div id="cnicContainer" style="display:none;">
          <label class="block font-semibold">CNIC</label>
          <input type="text" name="cnic" class="w-full border rounded px-3 py-2" />
        </div>

        <div id="passportContainer" style="display:none;">
          <label class="block font-semibold">Passport Number</label>
          <input type="text" name="passport" class="w-full border rounded px-3 py-2" />
        </div>

        <label class="block font-semibold">Payment Method</label>
        <select name="paymentMethod" required class="w-full border rounded px-3 py-2">
          <option value="">Select Payment Method</option>
          <option value="Credit Card">Credit Card</option>
          <option value="Debit Card">Debit Card</option>
          <option value="Cash">Cash</option>
          <option value="Bank Transfer">Bank Transfer</option>
        </select>

        <button type="submit" class="w-full bg-red-500 text-white font-bold py-3 rounded hover:bg-red-600 transition">
          Confirm Booking
        </button>
      </form>
    <% } else { %>
      <p class="text-center text-gray-500">Home details not found.</p>
    <% } %>
  </main>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/intl-tel-input/17.0.19/js/intlTelInput.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/intl-tel-input/17.0.19/js/utils.js"></script>
  <style>
    .iti {
      width: 100%;
      display: block;
    }
    .iti__flag-container {
      position: absolute;
      top: 0;
      bottom: 0;
      padding: 1px;
      background-color: #fff;
      border: 1px solid #ccc;
      border-radius: 4px 0 0 4px;
    }
    .iti__selected-flag {
      padding: 0 6px 0 8px;
      display: flex;
      align-items: center;
      cursor: pointer;
    }
    .iti__country-list {
      position: absolute;
      z-index: 1000;
      list-style: none;
      text-align: left;
      padding: 0;
      margin: 0;
      background-color: white;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.2);
      max-height: 200px;
      overflow-y: scroll;
      width: 300px;
      top: 100%;
      left: 0;
    }
    .iti__country {
      padding: 5px 10px;
      cursor: pointer;
      display: flex;
      align-items: center;
      white-space: nowrap;
    }
    .iti__country:hover {
      background-color: #f5f5f5;
    }
    .iti__flag {
      margin-right: 6px;
    }
    .iti__dial-code {
      color: #999;
      margin-left: 6px;
    }
    #phone {
      width: 100%;
      height: 100%;
    }
  </style>
  <script>
    document.addEventListener("DOMContentLoaded", function() {
      const phoneSelect = document.querySelector("#phone");
      const phoneNumber = document.querySelector("#phoneNumber");
      const phoneFormat = document.querySelector("#phoneFormat");

      // Phone number formats for different countries
      const phoneFormats = {
        "+92": { // Pakistan
          pattern: /^[0-9]{10}$/,
          format: "3XX-XXXXXXX",
          example: "3001234567",
          message: "Enter 10 digits (e.g., 3001234567)"
        },
        "+1": { // US/Canada
          pattern: /^[0-9]{10}$/,
          format: "(XXX) XXX-XXXX",
          example: "1234567890",
          message: "Enter 10 digits (e.g., 1234567890)"
        },
        "+44": { // UK
          pattern: /^[0-9]{10,11}$/,
          format: "XXXX XXXXXX",
          example: "7123456789",
          message: "Enter 10-11 digits (e.g., 7123456789)"
        },
        "+61": { // Australia
          pattern: /^[0-9]{9}$/,
          format: "X XXX XXX XXX",
          example: "412345678",
          message: "Enter 9 digits (e.g., 412345678)"
        },
        "+91": { // India
          pattern: /^[0-9]{10}$/,
          format: "XXXXX XXXXX",
          example: "9876543210",
          message: "Enter 10 digits (e.g., 9876543210)"
        }
      };

      // Update format message when country changes
      phoneSelect.addEventListener('change', function() {
        const countryCode = this.value;
        if (phoneFormats[countryCode]) {
          phoneFormat.textContent = phoneFormats[countryCode].message;
          phoneNumber.placeholder = `Example: ${phoneFormats[countryCode].example}`;
        } else {
          phoneFormat.textContent = "Please select a country code";
          phoneNumber.placeholder = "Enter phone number";
        }
        phoneNumber.value = ''; // Clear the input when country changes
      });

      // Format phone number as user types
      phoneNumber.addEventListener('input', function(e) {
        let value = e.target.value.replace(/\D/g, '');
        const countryCode = phoneSelect.value;
        
        if (phoneFormats[countryCode]) {
          const format = phoneFormats[countryCode];
          
          // Apply formatting based on country
          if (countryCode === "+92") { // Pakistan
            if (value.length > 3) {
              value = value.slice(0, 3) + "-" + value.slice(3);
            }
          } else if (countryCode === "+1") { // US/Canada
            if (value.length > 6) {
              value = `(${value.slice(0, 3)}) ${value.slice(3, 6)}-${value.slice(6, 10)}`;
            } else if (value.length > 3) {
              value = `(${value.slice(0, 3)}) ${value.slice(3)}`;
            }
          } else if (countryCode === "+44") { // UK
            if (value.length > 4) {
              value = value.slice(0, 4) + " " + value.slice(4);
            }
          } else if (countryCode === "+61") { // Australia
            if (value.length > 1) {
              value = value.slice(0, 1) + " " + value.slice(1);
            }
            if (value.length > 5) {
              value = value.slice(0, 5) + " " + value.slice(5);
            }
          } else if (countryCode === "+91") { // India
            if (value.length > 5) {
              value = value.slice(0, 5) + " " + value.slice(5);
            }
          }
        }
        
        e.target.value = value;
      });

      // Validate phone number before form submission
      const form = document.querySelector('#bookingForm');
      if (form) {
        form.addEventListener('submit', function(e) {
          e.preventDefault();
          const countryCode = phoneSelect.value;
          const number = phoneNumber.value.replace(/\D/g, '');
          
          if (!countryCode) {
            alert('Please select a country code');
            return;
          }

          if (!phoneFormats[countryCode].pattern.test(number)) {
            alert(`Please enter a valid phone number for the selected country.\n${phoneFormats[countryCode].message}`);
            return;
          }

          const fullNumber = countryCode + number;
          const hiddenInput = document.createElement('input');
          hiddenInput.type = 'hidden';
          hiddenInput.name = 'fullPhone';
          hiddenInput.value = fullNumber;
          form.appendChild(hiddenInput);
          form.submit();
        });
      }

      // Show/hide CNIC and Passport fields based on nationality selection
      const nationalitySelect = document.getElementById("nationality");
      const cnicContainer = document.getElementById("cnicContainer");
      const passportContainer = document.getElementById("passportContainer");

      function updateIdFields() {
        const val = nationalitySelect.value.toLowerCase();
        if(val === "pakistan") {
          cnicContainer.style.display = "block";
          passportContainer.style.display = "none";
        } else if(val) {
          cnicContainer.style.display = "none";
          passportContainer.style.display = "block";
        } else {
          cnicContainer.style.display = "none";
          passportContainer.style.display = "none";
        }
      }

      nationalitySelect.addEventListener("change", updateIdFields);
      updateIdFields();
    });
  </script>
</body>
</html>
